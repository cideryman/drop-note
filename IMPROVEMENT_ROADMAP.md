# 드립노트 개선 로드맵

> 작성일: 2026-07-31  
> 상태: THEME-01 완료 · CUSTOM-ACTION-01 대기
> 목적: 레시피 정확도, 추출 중 사용성, 데이터 및 타이머 안정성을 우선순위에 따라 개선한다.

## 기본 원칙

- 유명 레시피는 원본 출처와 수치를 보존하고, 수정한 경우 `앱 변형`으로 명시한다.
- 표시 비율, 단계별 물량, 최종 누적 중량은 항상 같은 계산 결과를 사용한다.
- HOT과 ICE는 단순 토글 변환이 아니라 서로 독립된 레시피로 취급한다.
- 타이머는 단계의 배열 순서가 아니라 실제 추출 시각을 기준으로 동작한다.
- 기능 추가보다 추출 중 잘못된 안내를 제거하는 작업을 먼저 한다.

## 실행 순서

| 순서 | ID | 작업 | 우선도 | 선행 작업 | 상태 |
|---:|---|---|:---:|---|---|
| 1 | RCP-01 | 기본 프리셋 수치와 출처 교정 | P0 | 없음 | 완료 |
| 2 | RCP-02 | HOT/ICE 계산 및 데이터 모델 분리 | P0 | RCP-01 | 완료 |
| 3 | CORE-01 | 절대 시각 기반 레시피 단계 스키마 도입 | P0 | RCP-01 | 완료 |
| 4 | CORE-02 | 실제 물량 기반 비율 계산과 입력 검증 | P1 | CORE-01 | 완료 |
| 5 | DATA-01 | JSON 가져오기 검증 및 안전한 렌더링 | P1 | CORE-01 | 완료 |
| 6 | NAME-01 | 프리셋 이름과 제작자·도구·변형 메타데이터 분리 | P2 | DATA-01 | 완료 |
| 7 | TIMER-01 | 타이머 드리프트 및 Wake Lock 복구 처리 | P1 | CORE-01 | 완료 |
| 8 | UX-01 | 인터페이스, 언어, 접근성 정리 | P2 | RCP-02, NAME-01 | 완료 |
| 9 | QA-01 | 핵심 계산·저장·타이머 테스트 추가 | P1 | CORE-02, DATA-01, TIMER-01 | 완료 |
| 10 | ARCH-01 | 단일 HTML 코드 모듈화 및 배포 의존성 정리 | P2 | QA-01 | 완료 |
| 11 | PWA-01 | 홈 화면 설치·오프라인 실행·iOS 복귀 흐름 | P2 | ARCH-01 | 완료 |
| 12 | FOCUS-01 | 초집중 추출 화면과 권장 온도 보조색 | P1 | UX-01, TIMER-01 | 완료 |
| 13 | GAUGE-01 | 온도색 현재 단계 시간 게이지 | P2 | FOCUS-01 | 완료 |
| 14 | TEMP-01 | 온도 전환 사전 준비 안내 | P1 | GAUGE-01 | 완료 |
| 15 | ACTION-01 | 비차단형 동작 알림 배너 | P2 | TEMP-01 | 완료 |
| 16 | GRIND-01 | 코만단테 C40 분쇄도 시작 범위 | P1 | RCP-01 | 완료 |
| 17 | THEME-01 | 라이트·시스템 테마 지원 | P2 | ACTION-01 | 완료 |
| 18 | CUSTOM-ACTION-01 | 커스텀 스월링·교반 동작 선택 | P2 | ACTION-01 | 대기 |

## 1. RCP-01 — 기본 프리셋 교정

### 현재 문제

- Kasuya 4:6은 `20g / 255g`이지만 화면에는 `1:15`로 표시된다.
- Hario Switch Sweet는 `20g / 200g`이지만 화면에는 `1:14`로 표시된다.
- James Hoffmann Ultimate는 총량은 맞지만 bloom 물량과 총시간이 원형과 다르다.
- 아이스 급랭은 단계별 뜨거운 물, 얼음, 표시 총량 및 비율이 서로 맞지 않는다.
- 분쇄도 클릭 수가 원두, 로스팅, 그라인더 영점 차이를 고려하지 않고 고정값으로 제시된다.

### 작업 범위

- 각 프리셋에 `출처`, `원본/변형`, `장비`, `기준 원두량`, `총 물량`, `권장 범위`를 기록한다.
- Kasuya 4:6 기본형을 `20g / 300g`, 60g씩 5회 주입하는 균형형 기준으로 교정한다.
- Switch 레시피의 정확한 이름과 원본을 확정하고 물량·온도·밸브 시점을 교정한다.
- Hoffmann Ultimate를 원형 기준으로 교정하거나 15g용 변형임을 명시한다.
- 아이스 레시피는 사용할 원본 레시피를 먼저 선정한 후 다시 작성한다.

### 참고 출처

- [HARIO 공식 4:6 Method 영상](https://www.youtube.com/watch?v=wmCW8xSWGZY)
- [Hario USA — James Hoffmann Ultimate V60](https://www.hario-usa.com/blogs/recipes-and-more-from-friends/james-hoffmann-uitimate-v60-technique)
- [Tetsu Kasuya Switch God/Devil 레시피 정리](https://klaire.codes/coffee/coffee/tetsu-kasuya-hario-switch-god-devil/)

### 완료 조건

- 모든 프리셋에서 `원두량 × 표시 비율 = 전체 물량`이 성립한다.
- 마지막 단계 누적 목표가 레시피 총 물량과 일치한다.
- 원본과 다른 값은 화면과 데이터에 `앱 변형`으로 표시된다.
- V60 레시피에는 Switch 상태가 표시되지 않는다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `drip_coffee_app_handoff.md`, `tests/recipe-presets.test.mjs`, `tests/calculation.test.mjs`
- 검증 방법: 기본 프리셋 4종의 원두량·추출수·시간·출처 메타데이터 검사, HOT/ICE 추출수·얼음·마지막 누적 목표 계산 회귀 테스트, 인라인 JavaScript 문법 검사
- 남은 문제: 원본/앱 변형 배지와 HOT/ICE 선택 구조는 `RCP-02`, 절대 시각 기반 단계 전환은 `CORE-01`에서 처리
- 관련 커밋: `18e4aa2`

## 2. RCP-02 — HOT/ICE 모델 분리

### 현재 문제

ICE 모드가 단계별 물량은 그대로 둔 채 얼음을 추가하여, 화면 총량과 실제 사용량이 달라진다.

### 작업 범위

- HOT/ICE 토글을 레시피 필터 또는 공식 변형 선택 기능으로 변경한다.
- 아이스 레시피에 `hotWaterTotal`, `iceWeight`, `finalWaterTotal`을 명시한다.
- 단계별 주입 합계는 반드시 `hotWaterTotal`과 일치하도록 한다.

### 완료 조건

- `단계별 뜨거운 물 합계 + 얼음 = 최종 전체 물량`이 성립한다.
- 핫 레시피를 ICE로 전환했을 때 임의로 얼음만 추가되지 않는다.
- 화면, 단계 목록, 타이머가 동일한 물량을 안내한다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `drip_coffee_app_handoff.md`, `tests/recipe-presets.test.mjs`, `tests/calculation.test.mjs`
- 검증 방법: HOT 원본, HOT→ICE 앱 변형, 독립 ICE 프리셋의 추출수·얼음·최종 예상량·마지막 누적 목표 및 모드 배지 회귀 테스트
- 남은 문제: 공식 HOT/ICE 쌍을 연결하는 `hotVariantId`/`iceVariantId` 데이터는 실제 공식 아이스 프리셋을 추가할 때 적용
- 관련 커밋: `18e4aa2`

## 3. CORE-01 — 레시피 단계 스키마 개선

### 현재 문제

`time`이 단계 지속시간과 동작 시작 시각을 동시에 의미하여 원본 타임라인을 정확히 표현하기 어렵다.

### 목표 형태

```js
{
  atSec: 75,
  action: "close-switch",
  pourAmount: 160,
  pourTo: 280,
  temperature: 70
}
```

### 작업 범위

- `atSec`, `durationSec`, `pourAmount`, `pourTo`를 분리한다.
- `pour`, `wait`, `swirl`, `stir`, `open-switch`, `close-switch`, `finish` 동작을 표현한다.
- 장비별로 불필요한 필드는 렌더링하지 않는다.
- 기존 커스텀 레시피를 새 스키마로 변환하는 마이그레이션을 준비한다.

### 완료 조건

- 0:45, 1:15, 1:45 같은 원본 시점을 데이터에 직접 기록할 수 있다.
- 0g 물을 사용하는 대기·스월·밸브 단계도 정상적으로 생성할 수 있다.
- 타이머가 데이터의 절대 시각과 같은 순간에 다음 안내를 표시한다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `drip_coffee_app_handoff.md`, `tests/recipe-presets.test.mjs`, `tests/calculation.test.mjs`
- 검증 방법: 프리셋별 절대 시각 연속성, 주입/단계 종료 범위, 가이드 모드, 레거시 `time` 변환, 단계 경계 전환, 선형 권장 중량 및 타이머 화면 문구 회귀 테스트
- 남은 문제: 실제 시계 기준 드리프트 보정과 백그라운드 복귀 처리는 `TIMER-01`에서 진행
- 관련 커밋: `18e4aa2`

## 4. CORE-02 — 비율 계산과 입력 검증

### 작업 범위

- `ratioText` 수기 입력을 제거하거나 참고값으로만 취급한다.
- 실제 총 물량과 원두량으로 표시 비율을 자동 계산한다.
- 원두량을 레시피별 검증 범위로 제한하고 드리퍼 용량 경고를 추가한다.
- 단계별 반올림 후 마지막 누적값과 표시 총량이 달라지지 않도록 보정한다.
- 커스텀 레시피 저장 전에 물량, 온도, 시간, 단계 수를 검증한다.

### 완료 조건

- 어떤 허용 원두량에서도 표시 총량과 마지막 누적 목표의 차이가 0g이다.
- 비정상 숫자, 빈 단계, 음수, 범위 초과 입력을 저장할 수 없다.
- 5g부터 100g까지 무조건 허용하지 않고 레시피별 권장 범위를 안내한다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `drip_coffee_app_handoff.md`, `tests/calculation.test.mjs`
- 검증 방법: 프리셋별 허용 원두 범위의 모든 0.5g 값에서 HOT/ICE 추출수·마지막 누적 목표·최종 예상량 일치 검사, 범위 밖 원두량과 0.5g 미준수 입력 거부, 정상·비정상 커스텀 레시피 검증
- 남은 문제: 가져오기 JSON의 동일한 스키마 검증과 안전한 문자열 렌더링은 `DATA-01`에서 진행
- 관련 커밋: 이번 입력·데이터·명명 개선 묶음 커밋

## 5. DATA-01 — 가져오기와 렌더링 안전성

### 작업 범위

- 가져온 JSON의 레시피 및 단계 구조를 검증한다.
- 사용자 입력과 가져온 문자열은 `innerHTML` 대신 `textContent`로 표시한다.
- 지원하지 않는 백업 버전과 손상된 파일에 명확한 오류 메시지를 제공한다.
- LocalStorage 저장 실패 시 성공 처리하지 않는다.

### 완료 조건

- 임의 HTML이 포함된 레시피명과 단계명이 코드로 실행되지 않는다.
- 필수 필드가 누락된 백업은 저장 전에 거부된다.
- 가져오기 실패 후 기존 레시피가 손상되지 않는다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `tests/data-import.test.mjs`, `tests/calculation.test.mjs`, `drip_coffee_app_handoff.md`, `IMPROVEMENT_ROADMAP.md`
- 검증 방법: 정상·이전 형식·지원하지 않는 버전·필수 필드 누락·잘못된 URL·중복 ID·HTML 포함 문자열·LocalStorage 실패 회귀 테스트, 기존 계산 및 프리셋 테스트, 인라인 JavaScript 문법 검사
- 남은 문제: 백업 암호화·클라우드 동기화는 현재 범위에 포함하지 않으며, 프리셋 표시 명명 규칙은 `NAME-01`에서 진행
- 관련 커밋: 이번 입력·데이터·명명 개선 묶음 커밋

## 6. NAME-01 — 프리셋 명명과 메타데이터

### 표시 원칙

- 제작자가 있으면 제목을 `제작자 · 레시피 이름`으로 표시한다.
- 제작자가 없으면 `아이스 기본형`처럼 레시피 이름만 표시한다.
- 도구와 원본/변형 여부는 제목에 섞지 않고 별도 배지로 표시한다.
- `creator`, `recipeName`, `equipment`, `variantLabel`을 별도 필드로 관리한다.

### 목표 예시

```text
테츠 카스야 · 4:6       / V60    / 원본
테츠 카스야 · Devil     / Switch / 원본
제임스 호프만 · Ultimate / V60    / 15g 변형
아이스 기본형            / V60    / 앱 변형 1:10
```

### 완료 조건

- 네 기본 프리셋이 동일한 명명 규칙으로 표시된다.
- 제작자가 없는 레시피에 임의의 사람·브랜드 이름을 붙이지 않는다.
- 제목을 조합하지 않고도 제작자, 도구, 변형을 기준으로 정렬하거나 필터링할 수 있다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `tests/recipe-presets.test.mjs`, `tests/calculation.test.mjs`, `tests/data-import.test.mjs`, `drip_coffee_app_handoff.md`, `IMPROVEMENT_ROADMAP.md`
- 검증 방법: 네 프리셋의 `creator`·`recipeName`·`equipmentLabel` 필드, 제작자 유무에 따른 제목 조합, 기존 `name` 백업의 자동 이전, 새 메타데이터 백업 복원 회귀 테스트
- 남은 문제: 제작자·도구 기반 검색과 필터 UI는 레시피 목록 화면을 구현할 때 추가
- 관련 커밋: 이번 입력·데이터·명명 개선 묶음 커밋

## 7. TIMER-01 — 타이머 안정성

### 작업 범위

- `setInterval()` 호출 횟수 대신 시작 시각과 현재 시각의 차이로 경과 시간을 계산한다.
- 앱이 잠시 백그라운드에 있어도 복귀 시 실제 시각에 맞춰 복구한다.
- `visibilitychange` 시 필요한 경우 Wake Lock을 다시 요청한다.
- 완료 후 Resume·Skip 중복 동작을 막고 컨트롤 상태를 완료 상태로 바꾼다.
- Wake Lock, 진동, 소리를 지원하지 않는 환경에 상태 안내를 제공한다.

### 완료 조건

- 백그라운드 복귀 후 타이머 오차가 1초 이내다.
- 일시정지 시간은 경과 시간에 포함되지 않는다.
- 완료 이후 추가 인터벌이나 중복 완료 알림이 발생하지 않는다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `tests/calculation.test.mjs`, `drip_coffee_app_handoff.md`, `IMPROVEMENT_ROADMAP.md`
- 검증 방법: 45.5초 지연 콜백과 90.4초 백그라운드 복귀의 실제 시각 보정, 30초 일시정지 제외, Skip 이후 기준 시각 재설정, 완료 이후 인터벌·Resume·Skip 차단, visibility 복귀 시 Wake Lock 재요청 회귀 테스트
- 남은 문제: iOS에서 백그라운드 JavaScript 연속 실행은 보장하지 않으며, PWA 설치·오프라인·Web Push 범위는 `PWA-01`에서 진행
- 관련 커밋: 이번 브랜드·타이머·UX 개선 묶음 커밋

## 8. UX-01 — 인터페이스 정리

### 작업 범위

- 화면 문구를 한국어 중심으로 통일한다.
- 구현되지 않은 Recipes와 Logs 내비게이션을 숨기거나 구현한다.
- 아이콘 버튼에 접근 가능한 이름을 제공하고 label과 input을 연결한다.
- 화면 확대 제한을 제거하고 모든 주요 터치 영역을 최소 44×44px로 만든다.
- 레시피 카드에 출처와 `원본/앱 변형` 상태를 표시한다.
- 타이머 진행률을 시작 전 0%로 표시하고 시간 진행을 반영한다.
- Stop 또는 화면 이탈 시 실수 방지 흐름을 추가한다.

### 완료 조건

- 보이는 모든 버튼이 동작한다.
- V60과 Switch 화면에서 장비에 맞는 정보만 보인다.
- 모바일 한 손 조작과 화면 확대가 가능하다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `tests/calculation.test.mjs`, `tests/ux.test.mjs`, `drip_coffee_app_handoff.md`, `IMPROVEMENT_ROADMAP.md`
- 검증 방법: 한국어 문구·viewport 확대 허용·label 연결·dialog 역할·진행률 ARIA·주요 터치 영역 44px 이상 정적 검사, 타이머 종료 확인/취소 회귀 테스트, 390×844 브라우저에서 메인·모달·타이머 레이아웃과 일시정지 상태 직접 검사
- 남은 문제: Tailwind Play CDN 제거와 정적 스타일 빌드는 `ARCH-01`, 실제 레시피·기록 탭 구현은 별도 기능 범위로 남김
- 관련 커밋: 이번 브랜드·타이머·UX 개선 묶음 커밋

## 9. QA-01 — 테스트 추가

최소한 다음 항목을 자동 검사한다.

- 프리셋별 원두량·물량·비율 일치
- 단계별 합계와 마지막 누적 목표 일치
- 아이스의 뜨거운 물·얼음·전체 물량 일치
- 서로 다른 원두량에서 반올림 일관성
- 타이머 절대 시각과 단계 전환
- Pause, Resume, Skip, Stop, 완료 상태
- 정상·손상·악성 JSON 가져오기
- LocalStorage 마이그레이션과 저장 실패

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `tests/calculation.test.mjs`, `tests/data-import.test.mjs`, `tests/recipe-presets.test.mjs`, `tests/ux.test.mjs`, `tests/pwa.test.mjs`, `tests/load-app-script.mjs`
- 검증 방법: `npm test`로 계산·타임라인·입력·타이머·백업·악성 JSON·프리셋·UX·PWA 자동 회귀 테스트 6건 통과
- 남은 문제: 실제 iPhone Safari 설치는 배포 URL에서 최종 기기 확인 필요
- 관련 커밋: 이번 구조 개선 묶음 커밋

## 10. ARCH-01 — 코드 구조와 배포

현재 단일 HTML의 간편함은 유지하되 다음 단위로 분리한다.

```text
index.html
styles.css
recipes.js
calculator.js
timer.js
storage.js
tests/
```

- React 같은 대형 프레임워크 도입은 현재 범위에서 필요하지 않다.
- Tailwind Play CDN 대신 정적 CSS 빌드 또는 최소한의 자체 CSS를 사용한다.
- PWA 설치와 오프라인 지원은 `PWA-01`에서 진행한다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `styles.css`, `styles.input.css`, `tailwind.config.cjs`, `recipes.js`, `calculator.js`, `storage.js`, `timer.js`, `app.js`, `package.json`, `package-lock.json`
- 검증 방법: 모든 JavaScript 파일 문법 검사, `npm run build:css`, `npm test`, 390×844 브라우저에서 정적 CSS·계산·타이머·모바일 레이아웃 확인
- 남은 문제: Inter·JetBrains Mono·Material Symbols는 네트워크 사용 시 Google Fonts를 사용하고, 오프라인에서는 캐시 또는 시스템 폰트로 대체
- 관련 커밋: 이번 구조 개선 묶음 커밋

## 11. PWA-01 — 설치와 오프라인 사용

### 작업 범위

- 웹 앱 매니페스트, 앱 아이콘, `apple-touch-icon`, 독립 실행 모드를 구성한다.
- 서비스 워커로 앱 셸과 기본 프리셋을 캐시하여 오프라인에서도 추출을 시작할 수 있게 한다.
- iPhone의 홈 화면 추가 방법과 알림 권한 조건을 안내한다.
- 화면이 잠기거나 앱이 백그라운드에 있는 동안 타이머 JavaScript가 계속 실행된다고 가정하지 않는다.
- 앱 복귀 시 저장한 시작 시각과 현재 시각의 차이로 추출 진행 상태를 복원한다.
- 필요하면 홈 화면 웹 앱용 Web Push 단계 알림을 별도 서버 기능으로 검토한다.

### iOS 범위

- PWA 단독으로 Dynamic Island의 Live Activity를 제공하지 않는다.
- Dynamic Island가 제품 필수 요건이 되면 ActivityKit·WidgetKit 기반 네이티브 iOS 앱 또는 네이티브 동반 앱을 별도 검토한다.

### 완료 조건

- iPhone 홈 화면에서 `드립노트` 이름과 전용 아이콘으로 실행된다.
- 네트워크 없이 앱과 기본 프리셋을 열 수 있다.
- 백그라운드에서 복귀한 직후 실제 경과 시각에 맞는 단계와 남은 시간이 표시된다.
- 지원하지 않는 백그라운드 연속 실행이나 Dynamic Island 표시를 사용자에게 약속하지 않는다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `manifest.json`, `service-worker.js`, `icon.svg`, `icon-180.png`, `icon-192.png`, `icon-512.png`, `app.js`, `styles.css`
- 검증 방법: 앱 셸 설치 후 로컬 서버를 종료하고 CSS·분리된 JavaScript·기본 프리셋·저장 레시피가 서비스 워커 캐시만으로 다시 열리는지 확인
- 남은 문제: iOS Dynamic Island Live Activity는 PWA 범위 밖이며, 실제 기기의 홈 화면 설치 아이콘·실행 화면은 배포 후 최종 확인 필요
- 관련 커밋: PWA·레시피 편집 및 이번 구조 개선 묶음 커밋

## 12. FOCUS-01 — 초집중 추출 화면

### 작업 범위

- 추출 중 앱 헤더와 부가 기능을 숨겨 시간, 현재 권장 저울 목표, 행동 문장에 시선을 집중시킨다.
- 정보 위계를 `진행 상태 → 경과 시간 → 권장 저울 목표 → 권장 물 온도·기구 상태 → 현재 행동 → 다음 행동` 순서로 정리한다.
- 권장 저울 목표를 가장 큰 숫자로 표시하되 실제 저울값으로 오해할 표현은 사용하지 않는다.
- 물 온도는 정확한 숫자와 온도계 아이콘을 항상 함께 표시하고 색은 보조 신호로만 사용한다.
- 온도색은 `70℃대 파랑`, `80℃대 청록`, `87–92℃ 앰버`, `93℃ 이상 코랄`의 네 구간으로 나눈다.
- 순수한 빨강은 오류 상태와 혼동되므로 온도 표현에 사용하지 않는다.
- 깜빡임과 반복 펄스 애니메이션을 사용하지 않는다.

### 완료 조건

- 390×844 화면에서 목표 무게, 행동 문장, 다음 행동과 하단 제어가 스크롤 없이 보인다.
- 온도색이 달라도 `권장 물 온도 70℃`처럼 문자만으로 의미를 이해할 수 있다.
- 타이머를 종료하면 계산기 헤더와 화면 구성이 원래 상태로 복원된다.
- 기존 절대 시각, 일시정지, 건너뛰기, 완료 및 백그라운드 복구 동작을 유지한다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `styles.input.css`, `styles.css`, `calculator.js`, `timer.js`, `service-worker.js`, `package.json`, `package-lock.json`, `tests/calculation.test.mjs`, `tests/data-import.test.mjs`, `tests/ux.test.mjs`
- 검증 방법: `npm run build:css`, `npm test`, 390×844 브라우저에서 92℃ 일반 단계와 70℃ Switch 닫힘 단계의 전체 화면·스크롤·제어 영역 직접 확인
- 후속 결과: 단계 게이지 `GAUGE-01`과 온도 준비 `TEMP-01` 완료, 비차단형 동작 알림 배너 `ACTION-01` 대기
- 관련 커밋: 초집중 모드·Switch 상태 구분 개선 커밋
- 추가 개선: Switch 열림은 `↓ 열림 · 배출`, 닫힘은 `━ 닫힘 · 침출`로 기호·문자·색을 모두 구분하고 단계명의 끝 괄호 설명은 별도 보조 줄로 분리

## 13. GAUGE-01 — 온도색 현재 단계 시간 게이지

### 작업 범위

- 목표 무게 카드의 하단 테두리에 현재 단계 전용 진행 게이지를 배치한다.
- 상단 진행 막대는 전체 추출 진행률, 카드 하단 게이지는 현재 단계 진행률로 역할을 구분한다.
- 게이지는 현재 단계의 권장 온도색을 사용하고 왼쪽에서 오른쪽으로 채운다.
- 내부 물 채움, 물결, 반복 이동 효과와 별도의 큰 남은 시간 숫자는 추가하지 않는다.
- 카드 배경과 목표 무게를 가리지 않는 4px 높이로 제한한다.

### 계산과 복구

```text
현재 단계 진행률 =
(실제 경과 시간 - 현재 단계 startSec)
÷
(현재 단계 stepEndSec - 현재 단계 startSec)
```

- 계산 결과는 0–100%로 제한하여 30초·45초 등 서로 다른 단계 길이에 자동으로 맞춘다.
- 화면은 250ms 주기로 부드럽게 갱신하되 진행률 기준은 인터벌 횟수가 아니라 실제 경과 밀리초를 사용한다.
- 일시정지하면 게이지도 멈추고, 재개·단계 건너뛰기·백그라운드 복귀 시 절대 시각으로 위치를 다시 계산한다.
- 단계가 바뀌는 시점에 게이지는 0%로 돌아가고 새 단계의 권장 온도색으로 변경된다.
- 스크린 리더에는 `현재 단계 진행률`과 반올림한 백분율을 제공한다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `styles.input.css`, `styles.css`, `timer.js`, `service-worker.js`, `package.json`, `package-lock.json`, `tests/calculation.test.mjs`, `tests/ux.test.mjs`
- 검증 방법: `npm run build:css`, `npm test`, 390×844 브라우저에서 게이지 높이·온도색·스크롤 없는 배치와 일시정지 시 진행률 고정 확인
- 후속 결과: 온도 전환 사전 준비 문구 `TEMP-01` 완료
- 관련 커밋: 단계 게이지·온도 준비·동작 배너 개선 커밋

## 14. TEMP-01 — 온도 전환 사전 준비 안내

### 목적

단계별 권장 온도가 크게 달라지는 레시피에서 현재 행동을 방해하지 않으면서 다음 단계의 물 온도를 준비할 시간을 제공한다.

### 표시 조건과 계산

- 현재 단계와 바로 다음 단계의 `temp` 차이가 5℃ 이상일 때만 준비 안내 후보로 판단한다.
- 기본 준비 시간은 다음 단계 시작 30초 전이다.
- 준비 안내 시각은 `max(현재 단계 pourEndSec, 다음 단계 startSec - 30)`으로 계산한다.
- 계산된 준비 구간이 5초보다 짧으면 별도의 준비 문구를 표시하지 않고 기존 다음 단계 미리보기만 유지한다.
- 첫 구현은 기존 단계 데이터에서 자동 계산하며 별도 필드를 필수로 추가하지 않는다.
- 레시피별 조정이 필요해지면 후속으로 선택 필드 `temperaturePrepLeadSec`를 추가하되 기본값은 30초로 한다.

### 화면 상태 전환

예: 0:30에 90℃ 주입 시작, 0:40에 주입 종료, 1:15에 70℃ 단계 시작

```text
0:30–0:40  현재 행동   120g까지 바로 주입하세요
0:40–0:45  완료/대기   주입 완료 · 1:15까지 기다리세요
0:45–1:15  사전 준비   다음 단계 준비 · 1:15에 사용할 권장 70℃ 물을 준비하세요
1:15–      다음 행동   280g까지 바로 주입하세요
```

- 준비 문구가 나타나도 큰 권장 저울 목표는 현재 단계 값을 유지하며 다음 목표를 미리 표시하지 않는다.
- 준비 안내에는 `다음 단계 준비`, 적용 시각, `권장` 온도를 함께 표시한다.
- 준비 안내는 현재 행동 문장 영역을 사용하되 목표 무게, 경과 시간, 현재 기구 상태와 하단 제어를 가리지 않는다.
- 온도 준비에는 소리, 진동, 깜빡임 또는 반복 애니메이션을 사용하지 않는다.
- 다음 단계에 Switch 변경이 함께 있어도 미리 조작하라고 안내하지 않는다. 하단 미리보기에는 예정 시각만 표시하고 정확한 시점의 조작은 `ACTION-01`이 담당한다.

### 추출 시작 전 안내

- 레시피에 5℃ 이상의 온도 전환이 하나라도 있으면 타이머 시작 전에 온도 변화 시각과 값을 요약한다.
- 예: `이 레시피는 1:15에 권장 온도가 90℃ → 70℃로 바뀝니다. 저온수를 준비할 방법을 확인해 주세요.`
- 실제 온도를 측정하는 기능이 아니므로 항상 `권장 온도`라고 표현한다.

### 완료 조건

- 현재 주입 중에는 미래 온도 준비 문구가 주요 행동을 대체하지 않는다.
- 90℃에서 70℃로 바뀌는 기본 Switch 레시피에서 0:45에 준비 안내가 시작되고 1:15에 실제 단계 안내로 전환된다.
- 준비 구간 동안 권장 저울 목표가 다음 단계 목표로 선행 변경되지 않는다.
- 동일 온도 또는 5℃ 미만 변화에는 준비 안내가 나타나지 않는다.
- 일시정지, 단계 건너뛰기와 백그라운드 복귀 후에도 절대 경과 시각을 기준으로 올바른 문구가 표시된다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `calculator.js`, `timer.js`, `service-worker.js`, `package.json`, `package-lock.json`, `styles.css`, `tests/calculation.test.mjs`, `tests/ux.test.mjs`
- 검증 방법: `npm run build:css`, `npm test`, 390×844 브라우저에서 시작 전 90℃→70℃ 요약, 0:45 준비 문구와 현재 목표·색 유지, 1:15 실제 저온 단계 전환 및 콘솔 오류 없음 확인
- 남은 문제: 정확한 시점의 Switch·스월링·교반 배너는 `ACTION-01`에서 진행
- 관련 커밋: 단계 게이지·온도 준비·동작 배너 개선 커밋

## 15. ACTION-01 — 비차단형 동작 알림 배너

### 결정 사항

- Switch 열기·닫기, 스월링, 교반처럼 사용자의 물리적 행동이 필요한 이벤트에만 사용한다.
- 전면 모달이나 화면 딤 처리를 사용하지 않고 목표 무게와 타이머를 가리지 않는 작은 배너로 표시한다.
- 배너가 보이는 동안에도 모든 타이머 제어를 사용할 수 있어야 한다.
- 이벤트 시점에 단계당 한 번만 나타나며 1.5–2초 뒤 페이드아웃한다.
- 깜빡임을 사용하지 않으며, 필요한 경우 동작 아이콘을 한 번만 부드럽게 움직인다.
- 배너가 사라진 뒤에도 변경된 기구 상태는 현재 단계 정보에 남긴다.
- 가까운 이벤트는 겹쳐 쌓지 않고 순서대로 처리한다.

### 완료 조건

- 동작 배너가 목표 무게, 경과 시간, 현재 행동 및 하단 제어를 가리지 않는다.
- 동일 이벤트가 반복 렌더링되어도 한 번만 표시된다.
- `prefers-reduced-motion` 환경에서는 이동 애니메이션 없이 짧은 페이드만 사용한다.

### 진행 기록

- 완료일: 2026-07-31
- 변경 파일: `index.html`, `styles.input.css`, `styles.css`, `timer.js`, `service-worker.js`, `package.json`, `package-lock.json`, `tests/calculation.test.mjs`, `tests/data-import.test.mjs`, `tests/ux.test.mjs`
- 검증 방법: `npm run build:css`, `npm test`, 390×844 브라우저에서 Switch 닫힘·열림 배너 위치, 입력 비차단, 약 2초 자동 소멸, 제어 활성 상태와 콘솔 오류 없음 확인
- 남은 문제: 사용자 지정 레시피 편집 화면에서 `swirl/stir/agitate` 동작을 직접 선택하는 UI는 별도 확장 범위
- 관련 커밋: 단계 게이지·온도 준비·동작 배너 개선 커밋

## 16. GRIND-01 — 코만단테 C40 분쇄도 시작 범위

### 적용 원칙

- C40 기본 액슬의 영점에서 센 `표준 클릭`을 기준으로 표시한다.
- Red Clix 장착 시 같은 버 간격의 클릭 수가 약 2배가 됨을 보조 문구로 안내한다.
- 제작자·공식 레시피가 클릭 수를 제시하지 않은 경우 `공식값`으로 단정하지 않고 `앱 시작 범위`로 표시한다.
- 원두, 로스팅, 물, 투입량과 영점 차이에 따라 최종 분쇄도는 맛과 추출 시간으로 조정한다.
- 사용자 레시피의 기존 자유 입력값은 그대로 유지하고 코만단테 메타데이터가 없을 때 안전한 기본 설명을 표시한다.

### 프리셋 시작점

| 프리셋 | C40 표준 클릭 | 근거 표시 |
|---|---:|---|
| 테츠 카스야 · 4:6 | 28–32 | 앱 시작 범위 |
| 테츠 카스야 · Devil | 19–22 | 약 20클릭 레시피 참고값 |
| 제임스 호프만 · Ultimate 15g 변형 | 20–24 | 앱 시작 범위 |
| 아이스 기본형 | 18–22 | 아이스 앱 시작 범위 |

### 참고 출처

- [Comandante 공식 FAQ — 푸어오버 18–35클릭 및 영점 정의](https://comandantegrinder.com/pages/faq)
- [Comandante C40 공식 사용 설명서](https://comandantegrinder.com/downloads/c40_manual_EN.pdf)
- [Hario USA — James Hoffmann Ultimate V60의 중미세 기준](https://www.hario-usa.com/blogs/recipes-and-more-from-friends/james-hoffmann-uitimate-v60-technique)
- [RoastAroma — Devil Recipe의 코만단테 약 20클릭 참고](https://roastaroma.com/blog/tetsu-kasuyas-devil-recipe-for-the-hario-switch)

### 진행 기록

- 완료일: 2026-08-02
- 변경 파일: `recipes.js`, `calculator.js`, `index.html`, `service-worker.js`, `package.json`, `package-lock.json`, 테스트 및 문서
- 검증 방법: 기본 프리셋 데이터 테스트, 사용자 레시피 폴백 및 모바일 분쇄도 카드 시각 확인
- 남은 문제: 클릭 범위는 절대 정답이 아니며 원두별 다이얼인 기록 기능은 별도 확장 범위
- 관련 커밋: 코만단테 C40 분쇄도 기준 추가 커밋

## 17. THEME-01 — 라이트·시스템 테마 지원

### 작업 범위

- 기존 다크 테마를 유지하면서 `시스템 설정`, `라이트`, `다크` 세 가지 선택을 제공한다.
- 기본값은 운영체제의 `prefers-color-scheme` 설정을 따르고 사용자가 직접 선택하면 이를 우선한다.
- 선택값은 LocalStorage에 저장하여 다음 실행과 오프라인 실행에서도 유지한다.
- 고정 색상을 배경, 표면, 기본 문자, 보조 문자, 테두리, 그림자처럼 의미 기반 CSS 변수로 정리한다.
- 라이트 모드에서는 글래스 카드의 투명도·테두리·그림자와 발광 효과를 별도로 조정한다.
- 온도 구간의 의미는 유지하되 라이트 배경에서 충분한 대비가 나도록 파랑·청록·앰버·코랄 값을 조정한다.
- 현재 단계 게이지, 온도 준비 강조, Switch 상태와 동작 배너가 색만으로 구분되지 않도록 기존 아이콘과 문자를 유지한다.
- 현재 테마에 맞춰 `<meta name="theme-color">`와 iPhone PWA 상태 표시줄 주변 색상을 동기화한다.

### 구현 순서

1. 현재 색상 사용처를 의미 기반 CSS 변수로 이전한다.
2. 다크 팔레트의 기존 시각 결과가 변하지 않는지 회귀 확인한다.
3. 라이트 팔레트와 시스템 테마 감지를 추가한다.
4. 헤더에 접근 가능한 테마 선택 UI를 추가하고 사용자 선택을 저장한다.
5. 시스템 테마가 바뀔 때 사용자가 `시스템 설정`을 선택한 경우에만 즉시 반영한다.
6. 계산기, 모달, 초집중 모드와 PWA 독립 실행 화면을 각각 검증한다.

### 완료 조건

- 첫 방문에는 시스템 테마가 적용되고 수동 선택은 새로고침·오프라인 재실행 후에도 유지된다.
- 라이트·다크 모두 주요 문자, 버튼, 온도색과 진행 게이지가 WCAG AA 수준의 대비를 만족한다.
- 테마 전환 중 타이머 상태, 현재 단계와 사용자 레시피 데이터가 초기화되지 않는다.
- iPhone Safari와 홈 화면 PWA에서 상태 표시줄 주변 색상이 현재 테마와 어울린다.
- `prefers-reduced-motion` 사용자는 테마 전환 애니메이션 없이 즉시 변경된다.

### 진행 기록

- 완료일: 2026-08-02
- 변경 파일: `theme.js`, `styles.input.css`, `tailwind.config.cjs`, `index.html`, `app.js`, `timer.js`, `service-worker.js`, 테스트 및 문서
- 검증 방법: 시스템·라이트·다크 저장 및 OS 변경 동기화 테스트, WCAG AA 색상 대비 검사, 전체 회귀 테스트, 390×844 라이트·다크 계산기·설정·커스텀 모달·초집중 타이머 확인
- 남은 문제: iPhone의 홈 화면 PWA 시작 화면은 정적 매니페스트 색상을 사용하며 문서 로드 후 활성 테마로 동기화됨
- 관련 커밋: 라이트·시스템 테마 지원 커밋

## 18. CUSTOM-ACTION-01 — 커스텀 스월링·교반 동작 선택

### 작업 범위

- 커스텀 레시피의 물 없는 단계에서 `대기`, `스월링`, `교반` 동작을 직접 선택할 수 있게 한다.
- 선택값을 단계의 `action` 필드로 저장하고 기존 동작 배너와 연결한다.
- 현재 구현된 단계별 Switch 열림·닫힘 선택과 상태 전환 감지는 유지하며 중복 UI를 추가하지 않는다.
- 기존 커스텀 레시피에서 `action`이 없으면 현재와 같이 물량 기준으로 `pour` 또는 `wait`로 이전한다.

### 완료 조건

- 사용자가 만든 스월링·교반 단계가 시작될 때 올바른 아이콘과 비차단형 배너가 한 번 표시된다.
- 기존 사용자 레시피와 JSON 백업을 수정 없이 가져올 수 있다.
- 물 주입 단계와 물 없는 동작 단계의 유효성 검사가 서로 충돌하지 않는다.

## 진행 기록 규칙

작업을 시작할 때 위 표의 상태를 `진행 중`으로 바꾸고, 완료 시 아래 내용을 남긴다.

```text
- 완료일:
- 변경 파일:
- 검증 방법:
- 남은 문제:
- 관련 커밋:
```

한 번에 하나의 P0/P1 작업만 `진행 중`으로 두며, 선행 작업의 완료 조건을 통과한 뒤 다음 작업으로 이동한다.
